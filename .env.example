# Three-Persona GTPS v2.1 - API Configuration
# Copy this file to .env and fill in your API keys.
#
# Each persona can use a different provider/model, or all three
# can point to the same API. Using different models per persona
# is NOT required — it's an option for experimentation.
#
# Supported providers: Any OpenAI-compatible API (OpenAI, Anthropic
# via proxy, local models via LM Studio / Ollama with OpenAI compat, etc.)

# ── Executor (generates work output under Clause 32 protocol) ──
REACT_APP_EXECUTOR_ENDPOINT=https://api.openai.com/v1/chat/completions
REACT_APP_EXECUTOR_API_KEY=sk-your-key-here
REACT_APP_EXECUTOR_MODEL=gpt-4

# ── Whistleblower (validates Executor compliance) ──
REACT_APP_WHISTLEBLOWER_ENDPOINT=https://api.openai.com/v1/chat/completions
REACT_APP_WHISTLEBLOWER_API_KEY=sk-your-key-here
REACT_APP_WHISTLEBLOWER_MODEL=gpt-4

# ── Proxy (companion commentary — NOT a filter) ──
REACT_APP_PROXY_ENDPOINT=https://api.openai.com/v1/chat/completions
REACT_APP_PROXY_API_KEY=sk-your-key-here
REACT_APP_PROXY_MODEL=gpt-4
